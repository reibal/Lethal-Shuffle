using UnityEngine;

public class BoardDropzone : MonoBehaviour
{
    // This component marks the area of the board for card drop detection.
    // It acts as a target identifier when using EventSystem.RaycastAll() from CardDisplay.
}
